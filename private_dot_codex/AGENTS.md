# Global Codex Instructions

Use the managed skills and project instructions available in the current workspace.

Never commit secrets, tokens, private keys, credentials, runtime histories, session logs, sqlite state, or `.env` files.

For chezmoi work, edit source files under the chezmoi source directory and verify with `chezmoi diff` before applying.
