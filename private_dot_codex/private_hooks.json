{
  "hooks": {
    "PostToolUse": [
      {
        "hooks": [
          {
            "command": "PATH=\"$HOME/bin:$HOME/.local/share/mise/shims:$PATH\"; command -v atuin >/dev/null 2>&1 && atuin hook codex || true",
            "type": "command"
          }
        ],
        "matcher": "^Bash$"
      }
    ],
    "PostToolUseFailure": [
      {
        "hooks": [
          {
            "command": "PATH=\"$HOME/bin:$HOME/.local/share/mise/shims:$PATH\"; command -v atuin >/dev/null 2>&1 && atuin hook codex || true",
            "type": "command"
          }
        ],
        "matcher": "^Bash$"
      }
    ]
  }
}
