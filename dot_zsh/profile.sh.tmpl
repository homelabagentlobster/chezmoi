# shellcheck shell=bash
# ~/.zsh/profile.sh
# Profile determination (templated by chezmoi)
# This file is generated by chezmoi based on .chezmoi.toml.tmpl
{{- $profile := .profile | default "" -}}
{{- with env "CHEZMOI_PROFILE" -}}
  {{- $profile = . -}}
{{- end }}
export PROFILE="{{ $profile }}"
