Create a Claude Code skill for this repeatable workflow.

The skill should include:
- name and description (frontmatter)
- when to use it
- when not to use it
- inputs to inspect first
- safe commands
- unsafe / ask first
- expected output
- known failure modes

Keep it scoped enough that Claude knows exactly when it applies.
